#include <iostream>
using namespace std;

//using Macro(takes 0 space just replaces the code at time of execution)
#define PI 3.14
//Don't use semi colon

int main()
{
    int r = 5;
    // double pi = 3.14;

    double area = PI * r * r;
    cout << "Area is " << area << endl;

    return 0;
}

//HW -geeks for geeks macro